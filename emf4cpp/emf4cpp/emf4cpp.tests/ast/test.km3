-- comment
-- comment




package test {

abstract class absclass {}

class abc extends absclass, absclass
{
        attribute a:String;
        attribute b[*]: Whatever;
        attribute c[1-*] : Whatother;
        attribute d[1-22] : Whatother;

reference parent[0-1] : Element oppositeOf children;
  reference children[*] ordered container : Node oppositeOf parent;

}


enumeration Color {
       literal green; -- Note: green
       literal blue;
       literal orange;
}

datatype my_string;
datatype other                  ; --other datatype.

 -- comment
}

